export type AdminMenuItem = {
  key: string;
  label: string;
  path: string;
  icon: string;
  mobileVisible: boolean;
  order?: number;
};

export const ADMIN_BRAND = 'HiMark 管理后台';

export const ADMIN_SUBTITLE = '统一超管登录 · 响应式 H5 后台';
