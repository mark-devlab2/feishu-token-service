import { AdminMenuItem } from '@config';

export const authCenterMenu: AdminMenuItem = {
  key: 'auth-center',
  label: '授权中心',
  path: '/auth-center',
  icon: 'auth',
  mobileVisible: true,
  order: 10,
};
